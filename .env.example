# Copie para .env e ajuste. Nunca versione o .env nem valores reais de chaves.

# Diretório de runtime/banco. Caminho relativo é resolvido a partir da raiz do projeto.
RAPM_DATA_DIR=data
RAPM_DB_FILENAME=ra_product_monitor.duckdb

RAPM_HOST=127.0.0.1
RAPM_PORT=8000

# Usada apenas nas fases que envolvem IA.
# ANTHROPIC_API_KEY=
